[project]
name = "usage-revenue-auditor"
version = "0.1.0"
description = "Finds unbilled revenue by reconciling contracts, product usage and invoices."
requires-python = ">=3.11"
dependencies = ["pydantic>=2.7"]

[project.scripts]
revaudit = "revaudit.cli:main"

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/revaudit"]

[tool.pytest.ini_options]
testpaths = ["tests"]
